---
schemaVersion: 2.0.0
name: "${prospero.dist.name}"
resolve-if-no-stream: none
manifest:
  maven:
    groupId: "${prospero.channel.manifest.groupId}"
    artifactId: "${prospero.channel.manifest.artifactId}"
repositories:
  - id: "${prospero.repo.id}"
    url: "${prospero.repo.url}"
